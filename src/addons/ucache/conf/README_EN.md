This directory stores the default configuration file (also an example).

Additional files will be added to this directory during integration.

It is integrated into the release package during release and can be modified using the deployment tool.
