# NPU plugin admission config for npu IT test - overrides the source conf version
# npu IT only needs npu_plugin; mem_plugin and mock_plugin are intentionally
# NOT listed here so they are not loaded during npu IT startup.
# npu_plugin for IT test. Default value: 781
npu_plugin=781
